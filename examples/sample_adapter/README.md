# Sample Subagent MCP adapter

This is a deterministic, separately packaged adapter that demonstrates the
public adapter API and Python entry-point discovery. It does not contact a
model provider.

Build it from this directory, install the resulting wheel beside
`subagent-harness-mcp`, then run the public conformance helper as shown in the
main [adapter authoring guide](../../docs/adapter-authoring.md).
